To run the application next time, you can simply open your terminal in the project folder and run:

powershell

.\run_app.bat



Alternatively, if you prefer using Docker:

powershell
docker-compose up --build
The app is still running in your background terminals from our previous steps: